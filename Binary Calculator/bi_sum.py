def unpacker(num):
    *num, = num # Unpack the given local variable.
    return num[::-1]

resulter = {"11": "1", "00": "0", "10": "1", "01": "1"}

result = []

num1 = unpacker("1001010111")
num1 = unpacker("1001110101")

if len(num1) == len(num2):
    for index, bit in enumerate(zip(num1, num2)) # Created O(n) can be improved further.
        
        

else:
    pass
